import { useNavigate } from "react-router-dom";

const navigate = useNavigate();

// use navigate("/Dashboard") to go redirect user to dashboard after user auth

const LoginPage = () => {
  return (
    <div>
      <H1>Login Here!</H1>
    </div>
  );
};
